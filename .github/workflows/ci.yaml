name: ci

on: [push]
#  push:
#    branches:
#      - master

jobs:
  build:
    name: build docker image
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      run:
        docker-compose build
  